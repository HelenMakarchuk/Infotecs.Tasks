<Page x:Class="Magazine.Application.Pages.SignUpPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Magazine.Application.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SignUpPage"
      Loaded="OnLoad">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <Border Grid.Column="1" Grid.Row="1" Background="#FFFFFF" Padding="20" BorderBrush="LightGray" BorderThickness="1">
            <StackPanel>
                <TextBlock Style="{StaticResource HeaderTextBlockStyle}">Infotecs Magazine</TextBlock>
                <TextBlock Style="{StaticResource BaseTextBlockStyle}">Login</TextBlock>
                <TextBox Name="Login" Style="{StaticResource LogInTextBoxStyle}"></TextBox>
                <TextBlock Style="{StaticResource BaseTextBlockStyle}">Password</TextBlock>
                <PasswordBox Name="Password" Style="{StaticResource LogInPasswordBoxStyle}"></PasswordBox>
                <Button Name="SignUpButton" Style="{StaticResource LogInButtonStyle}" Click="SignUpButton_Click">Sign Up</Button>
            </StackPanel>
        </Border>
    </Grid>

</Page>
