<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Magazine.Application">

    <!--#region TextBlock -->
    <Style x:Key="BaseTextBlockStyle" TargetType="TextBlock">
        <Setter Property="Margin" Value="0, 5" />
    </Style>

    <Style x:Key="HeaderTextBlockStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlockStyle}">
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="#004c8c" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="Margin" Value="0, 20" />
    </Style>
    <!--#endregion-->

    <!--#region TextBox -->
    <Style x:Key="BaseTextBoxStyle" TargetType="TextBox">
        <Setter Property="Padding" Value="5" />
    </Style>

    <Style x:Key="LogInTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource BaseTextBoxStyle}">
        <Setter Property="Margin" Value="0, 0, 0, 5" />
        <Setter Property="Background" Value="#FAFAFA" />
        <Setter Property="Foreground" Value="#262626" />
    </Style>

    <Style x:Key="EditTextBoxStyle" TargetType="TextBox">
        <Setter Property="Background" Value="White" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="LightGray" />
        <Setter Property="IsReadOnly" Value="False" />
    </Style>

    <Style x:Key="ReadOnlyTextBoxStyle" TargetType="TextBox">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="IsReadOnly" Value="True" />
    </Style>
    <!--#endregion-->

    <!--#region PasswordBox -->
    <Style x:Key="BasePasswordBoxStyle" TargetType="PasswordBox">
        <Setter Property="Padding" Value="5" />
    </Style>

    <Style x:Key="LogInPasswordBoxStyle" TargetType="PasswordBox" BasedOn="{StaticResource BasePasswordBoxStyle}">
        <Setter Property="Margin" Value="0, 0, 0, 5" />
        <Setter Property="Background" Value="#FAFAFA" />
        <Setter Property="Foreground" Value="#262626" />
    </Style>
    <!--#endregion-->

</ResourceDictionary>